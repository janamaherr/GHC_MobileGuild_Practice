// Task: Use Copilot to generate mock network responses for testing.
struct User: Codable {
    let id: Int
    let name: String
}

func fetchUserMock() -> User {
    // Ask Copilot to implement this.
}